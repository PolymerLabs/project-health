language: node_js
node_js: "8"
sudo: false
dist: trusty
cache:
  directories:
    - server/node_modules
install:
  - (cd server && npm install)
  - (cd client && npm install)
script:
  - (cd server && npm run test)
after_script:
  - (cd server && npm run report-coverage)
